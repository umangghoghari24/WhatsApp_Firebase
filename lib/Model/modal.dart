
class cotactModal{
  var time;
  var profilePic;
  String ? name;
  String ? message;
  bool isselected = false;

  cotactModal({
    this.time,
    this.profilePic,
    this.name,
    this.message,
    this.isselected=false
  });
}